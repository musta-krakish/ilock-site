---
import heroBackground from '../assets/images/hero-background.jpg';
import { getLocalePath, type Lang, ui } from '../i18n';

interface Props {
	lang?: Lang;
}

const { lang = 'ru' } = Astro.props;
const t = ui[lang];
const homePath = getLocalePath(lang);
---

<section class="snap-section relative isolate min-h-screen overflow-hidden bg-[#151414] pt-20 text-white">
	<img
		src={heroBackground.src}
		alt=""
		class="absolute inset-0 z-0 h-full w-full object-cover object-center opacity-65"
		fetchpriority="high"
	/>
	<div class="absolute inset-0 z-0 bg-[linear-gradient(90deg,#100E0E_0%,rgba(16,14,14,0.82)_34%,rgba(16,14,14,0.55)_58%,#100E0E_100%)]"></div>
	<div class="absolute inset-0 z-0 bg-[#100E0E]/15"></div>

	<div
		class="relative z-10 mx-auto flex min-h-[calc(100vh-80px)] max-w-7xl items-center px-6 py-16 sm:px-10 lg:px-24"
	>
		<div class="max-w-xl">
			<p class="mb-7 flex items-center text-sm font-medium text-[#DFA620]">
				<span class="mr-2 h-px w-16 bg-[#DFA620]"></span>
				{t.heroEyebrow}
			</p>

			<h1 class="text-5xl font-black leading-[1.08] text-white sm:text-6xl lg:text-7xl">
				{t.heroTitleFirst}
				<span class="mt-1 block">
					<span class="text-[#DFA620] italic">{t.heroTitleAccent}</span> {t.heroTitleLast}
				</span>
			</h1>

			<p class="mt-8 max-w-md text-base leading-7 text-white/58">
				{t.heroText}
			</p>

			<div class="mt-10 flex flex-col gap-4 sm:flex-row">
				<a
					href={`${homePath}#catalog`}
					class="inline-flex h-14 items-center justify-center rounded-none bg-[#DFA620] px-9 text-base font-medium text-[#100E0E] transition hover:bg-[#f0bd39] focus:outline-none focus:ring-2 focus:ring-[#DFA620] focus:ring-offset-2 focus:ring-offset-[#100E0E]"
				>
					{t.heroCatalog}
				</a>
				<a
					href={`${homePath}#contact`}
					class="inline-flex h-14 items-center justify-center rounded-none border border-[#DFA620]/70 px-9 text-base font-medium text-white transition hover:border-[#DFA620] hover:bg-[#DFA620]/10 focus:outline-none focus:ring-2 focus:ring-[#DFA620] focus:ring-offset-2 focus:ring-offset-[#100E0E]"
				>
					{t.heroContact}
				</a>
			</div>
		</div>
	</div>
</section>
